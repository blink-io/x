package strftime

import "github.com/ncruces/go-strftime"

var (
	Format       = strftime.Format
	AppendFormat = strftime.AppendFormat
	Parse        = strftime.Parse
	Layout       = strftime.Layout
	UTS35        = strftime.UTS35
)
