package render

import (
	"github.com/unrolled/render"
)

type (
	Engine          = render.Engine
	Render          = render.Render
	Options         = render.Options
	LocalFileSystem = render.LocalFileSystem
)
